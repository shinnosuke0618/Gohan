<div class='container'>
  <div class='row d-flex justify-content-around'>
    <div class="col-sm-12 col-md-8 col-lg-6 px-5 px-sm-0">

      <h2>新規会員登録</h2>
      <p class="title_border">こちらから新規登録お願い致します</p>

      <%= form_with model: @customer, url: customer_registration_path do |f| %>
      <%= render "public/shared/error_messages", resource: resource %>

      <div class="field form-item">
        <%= f.label :名前 %>
        <%= f.text_field :name, autofocus: true, placeholder:"例）ごはんくん", autocomplete: "name" %>
      </div>

      <div class="field form-item">
        <%= f.label :自己紹介文 %><br />
        <%= f.text_field :introduction, placeholder:"例）趣味は食べ歩きです", autocomplete: "introduction" %>
      </div>

      <div class="field form-item">
        <%= f.label :メールアドレス %><br />
        <%= f.email_field :email, placeholder:"sample@example.com", autocomplete: "email" %>
      </div>

      <div class="field form-item">
        <%= f.label :パスワード（６文字以上） %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "new-password" %>
      </div>

      <div class="field form-item">
        <%= f.label :パスワード（確認用） %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
      </div>

      <div class="actions button-panel">
        <%= f.submit "新規登録", class: 'button' %>
      </div>
    <% end %>

    <p class="pt-3 title_border">既にご登録済みの方</p>
    <p><%= link_to "こちら", new_customer_session_path %>からログインください。</p>

   </div>
  </div>
</div>